# Release

Authorized publication target: https://github.com/nielslindor/CaC/releases/tag/v0.2.0.dev0. The tag matches package version0.2.0.dev0 and the workflow labels development versions as prereleases. CI must succeed before publication; downloaded assets are checked after publication. This record specifies the release operation and is not a claim that the remote release already exists.

To roll back an installation, reinstall the previously pinned wheel and restart its controller. Keep host-local state, credentials and worktrees. Source rollback requires a reviewed revert with refreshed SDLC evidence. The prior stable release remains0.1.2; this development release does not replace stable coverage claims. Actual receiving-Mac deployment is outside the verified coverage.
