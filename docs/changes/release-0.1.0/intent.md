# Intent

Create a public reusable toolkit that initializes a separate agent workspace from declarative definitions and carries changes through a complete software development lifecycle with multiple bounded agents. The observable first result is a real Git repository, repeatable reconciliation, editable project records and independent verification. Public artifacts contain generic product material only.
