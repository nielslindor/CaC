# Verification

Local integrated suite: 39 tests pass across engine, lifecycle, gauntlet, bootstrap and GitHub adapter boundaries. Packaged wheel installed into a separate temporary virtual environment and working directory: bootstrap assets present; local Git created; second apply unchanged; ownership bytes stable; local drift preserved and refused; generated verification placeholders rejected. Independent reviewer reproduced Git metadata symlink containment issues; parent fixed preflight and added regression coverage. Source freeze, hosted CI and remote release observations are recorded by the release follow-through.

The real GitHub provider created the separately private target, attached origin, and then returned no changes on a repeated apply; fresh identity and visibility readback matched the declaration. Public-tree privacy scan and independent final packaging/CI review found no blocking findings.
