# Verification

39 regression tests passed. The 0.1.1 wheel installed in a separate temporary runtime, generated a workspace, committed and cloned it, observed the missing empty directory, reconciled and verified the clone, then completed a second unchanged apply with a clean Git worktree. An independent read-only reviewer checked the workflow authority boundary and actual clone proof and found no blocker. Hosted PR/release checks and the real instance readback complete delivery.

GitHub dependency scanning identified the build-only setuptools exclusion advisory GHSA-h35f-9h28-mq5c. The build pin was updated from 82.0.0 to patched 84.0.0; release CI rebuilds with this backend before publication.

Published release v0.1.1: https://github.com/nielslindor/CaC/releases/tag/v0.1.1. Hosted release workflow 33925537196 completed successfully. Downloaded wheel SHA-256: 67886e8659037ccdebe75223e7a19c0424cfe989b51e4ece7447a90a3bc330e9. Independent installation of that published wheel passed bootstrap, repeat idempotency, drift refusal, false-completion refusal and fresh-clone reconciliation. Source archive checksum and normalized ownership metadata were verified. A separate private instance passed its own hosted workflow after adopting the corrected manifest. Native investigator and reviewer roles were dispatched and returned using Codex CLI 0.153.3. The stored source digest identifies the tagged release; this later operational receipt does not redefine that artifact.
