# Spec

Persist explicit workspace versus public checking profiles. Workspace checks accept personal paths, private network addresses and ordinary binary documents; credential-like text findings and integrity/lifecycle failures remain errors. A public publication profile retains private-context checks. Owners choose the profile; repository visibility alone does not infer permission. Fix generated instructions and ignore rules, and adopt the change in the existing private instance through its manifest.
