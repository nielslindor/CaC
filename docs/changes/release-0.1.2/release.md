# Release

Target v0.1.2. Publish through required PR checks and the tagged release workflow, then install the published artifact and update the existing instance workflow pin. Existing repositories without cac-policy.json retain public checks; owners deliberately adopt a profile. The public product stays public-profiled. To roll back, restore the previous manifest in Git, review the plan, apply and verify; preserve the original release tags and private content.
