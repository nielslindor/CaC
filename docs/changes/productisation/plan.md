# Plan

Primary owns package integration, generic source extraction, build/install, acceptance and ledgers. Use bounded disjoint workers for saved-plan behavior and independent review. Develop in an isolated product checkout, record a real fleet ownership lease, preserve the existing private rollout channel, and build a private preview from generic product code before any public release. Validate the installed wheel from outside its checkout and run existing bootstrap regressions plus runtime tests.
