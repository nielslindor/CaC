# Plan

Implement the compatibility adapter in `src/codexascode/gauntlet.py`, add
focused fixtures in `tests/test_gauntlet.py`, then run the focused tests, the
full deterministic check, and a clean wheel installation check. Official
upstream field names and enum values are sourced from the current ChatGPT Learn
custom-agent and configuration reference pages.
